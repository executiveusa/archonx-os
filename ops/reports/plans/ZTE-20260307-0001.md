# ZTE PLAN: SECURITY HARDENING & TOKEN OPTIMIZATION
**ID:** ZTE-20260307-0001 | **Status:** IN_PROGRESS

## 1. Secret Vault (Phase 2)
The current `master.env` and `kilo-code-secrets.json` files contain over 180 plaintext secrets. 
- **Action**: Implement `archonx/security/vault.py` using Fernet symmetric encryption.
- **Storage**: Store encrypted blob in `.archonx/vault.bin` (git-ignored) or Supabase.
- **Key**: Key derived from a single `ARCHONX_MASTER_KEY` env var.

## 2. Token Optimization
- **Action**: Fix `jcodemunch-mcp` CLI.
- **Action**: Configure `ralphy` to use the new ZTE persona.

## 3. Vercel Monitoring
- **Action**: Track build status of `prj_T19WSaUiqLmrAewXECfctQyw4jKe`.
